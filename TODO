- ASAP

- BACKBURNER
    - Slot system to swap between different versions of database(s) without having to reimport.
    - Router catch redirect loops.
    - Check if .platform.app.yaml relationships actually match entries in .platform/services.yaml.
    - Delete commit command.
    - [PARTIAL COMPLETE] Docker mock tests.

- NEEDS MORE TESTING    
    - Workers.
    - Platform.sh clone script.
    - Implement service.yaml PCC override.