- ASAP

- QOL
    - Throw an error if upstream service does not exist when generating router Nginx.
    - Display current slot with project:status.

- BACKBURNER
    - Router catch redirect loops.
    - Check if .platform.app.yaml relationships actually match entries in .platform/services.yaml.
    - Restart command doesn't support save slots.

- NEEDS MORE TESTING
    - Platform.sh clone script.