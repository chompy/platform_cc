- Check if .platform.app.yaml relationships actually match entries in .platform/services.yaml
- Add support for !include YAML tag
- Add support for service relationships (Varnish)
- Fix app:sh command
- Implement old PCC style command with semicolon (:) command seperator
- Create main router