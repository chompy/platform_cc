vars:
    "env:ENVIRONMENT": dev
    "env:SYMFONY_ENV": dev
    "env:AWS_ACCESS_KEY_ID": AKIAINN6BYLRJKT7ORQA
    "env:AWS_SECRET_ACCESS_KEY": "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"

ssh_private_keys:
    - |
        -----BEGIN RSA PRIVATE KEY-----
        PRIVATE KEY
        -----END RSA PRIVATE KEY-----

assets:
    - handler: s3
      bucket: cc_backups
      path: mla/db-{CURRENT_MONTH}-{CURRENT_DAY}-{CURRENT_YEAR}.sql.gz
      to: db-mla.sql.gz

    - handler: s3
      bucket: cc_backups
      path: mla/var-{CURRENT_MONTH}-{CURRENT_DAY}-{CURRENT_YEAR}.sql.gz
      to: var-mla.tar.gz

    - handler: s3
      bucket: cc_backups
      path: "{PROJECT_DIRNAME}/db-{CURRENT_MONTH}-{CURRENT_DAY}-{CURRENT_YEAR}.sql.gz"
      to: db.sql.gz

    - handler: rsync
      from: "ssh://remote.server/var/test"
      to: "test/"

    - handler: http
      from: "ssh://remote.server/var/test"
      to: "test/"

tasks:
    - type: mysql_import
      path: db-mla.sql.sz
    
    - type: asset_s3
      bucket: cc_backups

    - type: shell
      command: |
            tar xvfz var-mla.tar.gz mount
      